import { AllImages } from '../../helpers';
import './Footer.scss';

export const Footer = () => {
    return (
        <div className="footer">
            <img src={AllImages.LogoFooter} alt="" />
        </div>
    )
}
